using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BugLevel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MusicMgr.Ins.PlayBGM("Level");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
